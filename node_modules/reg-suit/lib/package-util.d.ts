export declare const PLUGIN_NAME_REGEXP: RegExp;
export declare type NpmClient = "npm" | "yarn" | "yarn workspace";
export declare class PackageUtil {
    installPackages(client: NpmClient, packageNames: string[]): Promise<string[]>;
    installPluginAndCli(client: NpmClient, pluginNames: string[]): Promise<string[]>;
    checkInstalled(pkgName: string): string | null;
    checkInstalledLocalCore(): string | null;
    checkInstalledLocalCli(): string | null;
    getInstalledPlugins(): string[];
}
declare const packageUtil: PackageUtil;
export default packageUtil;
